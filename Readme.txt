
// cannot be loaded because running scripts is disabled on this system. For more information, see about_Execution_Policies at vue


// If ever run in to the above error go to adminstor as admin and run the below code
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass



//To Create a Vue JS app
first install vue cli  using
npm install -g @vue/cli

and check vue version using
vue --version


//This has to be done in the cmd which is not opened from the folder path but from using windows key
